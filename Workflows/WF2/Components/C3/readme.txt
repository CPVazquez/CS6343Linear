I have hard coded the coordinates for now. Will be adding test cases soon.

get_distance function -
	Requirements:
		python >= 3.8

	Steps to run:
		vim src/config.py	
		Add API_KEY to config.py
		python3 src/get_distance.py

